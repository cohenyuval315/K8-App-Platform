
# SOURCE_PATH="/cache/node_modules"
# DEST_PATH="/code"

# # Check if the source folder exists
# if [ -d "$SOURCE_PATH" ]; then
#   # If the folder exists, copy it to the destination
#   sudo cp -r "$SOURCE_PATH" "$DEST_PATH"
#   echo "Folder copied from $SOURCE_PATH to $DEST_PATH"
# else
#   # If the folder doesn't exist, do nothing
#   echo "Source folder $SOURCE_PATH does not exist. No action taken."
# fi
