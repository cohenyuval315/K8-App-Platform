const WebContainer = ({children}:{children:any}) => {
    return (
        <div style={{
            // display:"flex",
            // flex:1,
            // flexDirection:"column",
            // width:"100%",
            // justifyContent:"center",
            // alignItems:"center",
            // minHeight:"100"
        }}>
            {children}
        </div>
    )
}
export default WebContainer
